# Third-party notices

The native Court Tally app uses Apple's SwiftUI, Foundation, UIKit, and system SQLite libraries. It has no third-party runtime package dependencies and does not embed the Flutter engine or Dart libraries.

XcodeGen is an optional development tool for regenerating the checked-in Xcode project; it is not distributed in the app. The old Flutter dependency notices are preserved at `legacy/flutter/THIRD_PARTY_NOTICES.md` for historical and legacy-build use.

The app icon was supplied by the project owner. See `assets/branding/LICENSE.md` for provenance.
